# Автоматический деплой

Я подготовил несколько способов автоматического деплоя:

## 🚀 Способ 1: GitHub Actions (Рекомендуется)

Автоматический деплой при каждом push в main ветку.

### Настройка:

1. **Получите токены** (см. `.github/workflows/setup-vercel.md`)
2. **Добавьте секреты в GitHub**:
   - Settings → Secrets and variables → Actions
   - Добавьте все секреты из инструкции
3. **Сделайте push** - деплой запустится автоматически!

## 🛠️ Способ 2: Скрипты (Локальный деплой)

### Первоначальная настройка:

```bash
# Установка зависимостей
bash scripts/setup.sh
```

### Деплой фронтенда на Vercel:

```bash
bash scripts/deploy-vercel.sh
```

### Деплой бэкенда на Render:

```bash
bash scripts/deploy-render.sh
```

## 📝 Способ 3: Ручной деплой

Следуйте инструкциям в `VERCEL_SETUP.md` и `DEPLOY.md`

## ⚡ Быстрый старт

Если у вас уже есть аккаунты на Vercel и Render:

1. **Vercel**:
   ```bash
   cd frontend
   npm install -g vercel
   vercel login
   vercel --prod
   ```

2. **Render**:
   - Подключите репозиторий через веб-интерфейс
   - Настройки применятся автоматически из `render.yaml`

## 🔧 Переменные окружения

### Vercel:
- `VITE_BACKEND_URL`: `https://strategy-game-pvnb.onrender.com`

### Render:
- `ALLOWED_ORIGINS`: `https://strategy-game-4jwu.vercel.app,http://localhost:3000,http://localhost:5173`
- `PORT`: автоматически (не нужно устанавливать)

## ❓ Проблемы?

1. Проверьте логи в GitHub Actions
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все секреты добавлены правильно

