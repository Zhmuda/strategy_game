# 🚀 Быстрый деплой - 5 минут

## Что я подготовил:

✅ GitHub Actions workflow для автоматического деплоя  
✅ Скрипты для локального деплоя  
✅ Все конфигурационные файлы  
✅ Инструкции по настройке  

## ⚡ Самый быстрый способ (через веб-интерфейсы):

### 1. Render (Backend) - уже работает! ✅
Ваш бэкенд уже задеплоен на: https://strategy-game-pvnb.onrender.com

### 2. Vercel (Frontend) - нужно только настроить:

**Шаг 1**: Откройте https://vercel.com/new
- Подключите ваш GitHub репозиторий
- **Root Directory**: установите `frontend`
- **Framework Preset**: Vite (определится автоматически)

**Шаг 2**: Добавьте переменную окружения:
- **Name**: `VITE_BACKEND_URL`
- **Value**: `https://strategy-game-pvnb.onrender.com`
- **Environment**: Production, Preview, Development

**Шаг 3**: Нажмите **Deploy**

Готово! 🎉

## 🤖 Автоматический деплой (GitHub Actions):

После первого деплоя настройте автоматизацию:

1. **Получите токены** (см. `.github/workflows/setup-vercel.md`)
2. **Добавьте секреты в GitHub**:
   - Settings → Secrets → Actions
   - Добавьте: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`, `VITE_BACKEND_URL`
3. **Готово!** Теперь каждый push в main автоматически деплоит проект

## 📝 Что уже готово:

- ✅ `vercel.json` - конфигурация Vercel
- ✅ `render.yaml` - конфигурация Render  
- ✅ `.github/workflows/deploy.yml` - автоматический деплой
- ✅ `scripts/` - скрипты для ручного деплоя
- ✅ Все переменные окружения настроены в коде

## ❓ Если что-то не работает:

1. Проверьте консоль браузера (F12) - там будут ошибки
2. Убедитесь, что `VITE_BACKEND_URL` установлена в Vercel
3. Убедитесь, что Root Directory = `frontend` в Vercel
4. После изменения настроек - пересоберите проект (Redeploy)

## 🎯 Итого:

**Вам нужно сделать только:**
1. Подключить репозиторий к Vercel
2. Установить Root Directory = `frontend`
3. Добавить переменную `VITE_BACKEND_URL`
4. Нажать Deploy

**Все остальное уже готово!** 🚀

